git status
            git commit -am "update"   #### no usar esta linea
git add .
git commit -m "update"
git push origin master	

user: josegit88
pass: gi......2



pip install black
black scorpio.py
black test.py
black setup.py
tox -re style
touch pyproject.toml
nano pyproject.toml
black scorpio.py
tox -re style
black scorpio.py
black setup.py
black test.py
tox -re style
tox -r
pytest test.py
tox -re test
tox -re py38 -- -v
tox -re py37 -- -v

tox -re py36
pytest test.py -v

tox -re style
tox -re docs


import astropy.cosmology as asc
asc.Planck18_arXiv_v2
asc.FLRW
isinstance(asc.Planck15, asc.FLRW)
isinstance(asc.WMAP5, asc.FLRW)
asc.LambdaCDM(H0=70, Om0=0.3)
asc.LambdaCDM(H0=70, Om0=0.3, Ode0=0.0)
cosmo_propio = asc.LambdaCDM(H0=70, Om0=0.3, Ode0=0.0)
isinstance(cosmo_propio, asc.FLRW)


cd docs/
make html


Borrar files espureos de git:
https://stackoverflow.com/questions/14576109/how-to-delete-a-file-in-remote-repository-only

git rm --cached prueba_scorpio.py
git rm --cached prueba_scorpio_plot_kwargs.py
git rm --cached -r dir_test_images
git commit -m "Removed unnecessary folders and files from repository"
git push origin master
 
 
 

"""scorpio.

This program receives RA and Dec information from a pair of galaxies.
interacting (or nearby) or other individual objects and download the data
Corresponding .fits of survey data and list of filters:

******************************************************
        Survey              >>>      Filters
******************************************************
Sloan Digital sky Survey (SDSS) >>> [u, g, r, i, z]
Two Micron All Sky Survey (2MASS) >>> [J, H, K]
Wide Field Infrared Survey Explorer (WISE) >>> [3.4, 4.6, 12, 22]
Later it processes and exports an image.
For this it has a series of steps that use some specific purpose functions:
******************************************************
        Process              >>>      Function
******************************************************
Download .fits data          >>>    download_data
Stack the information        >>>      stack_pair
Calculate distance to the
observer Mpc and the
separation between the pair  >>>      distances
Generate the image           >>>      Image.plot
******************************************************
"""


#algunos comandos para subir el paquete a pypi y probarlo localmente:
conda create --name env_p1 python=3.7
source activate env_p1
python3 -m pip install --index-url https://test.pypi.org/project/scorpio/ --no-deps example-pkg-josepypi
python3 -m pip install --index-url https://test.pypi.org/project/scorpio/ 
python3 -m pip install --index-url https://test.pypi.org/project/scorpio/ --no-deps josepypi
python3 -m pip install --index-url https://test.pypi.org/project/scorpio/ --no-deps scorpio-josepypi
pip install -i https://test.pypi.org/simple/ scorpio
pip install retrying
pip install -i https://test.pypi.org/simple/ scorpio
pip install matplotlib
pip install -i https://test.pypi.org/simple/ scorpio
pip install astroquery
pip install -i https://test.pypi.org/simple/ scorpio
pip install -i https://test.pypi.org/simple/ scorpio-gp==0.0.1


En esta web se muestra rapidamente como acualizar en test.pypy y luego el pypy:
https://widdowquinn.github.io/coding/update-pypi-package/
